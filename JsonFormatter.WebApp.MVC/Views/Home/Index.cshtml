@model JsonFormatter.WebApp.MVC.Models.PageTextData

<div class="jumbotron">
    @Html.TextArea("inputTextArea", "This is value", new { @class = "form-control", rows = 10 })
</div>
<hr />
<div class="jumbotron">
    @Html.TextArea("resultTextArea", null, new { @class = "form-control", rows = 10 })
</div>

@using (Ajax.BeginForm("TestPage", new AjaxOptions { UpdateTargetId = "resultTextArea", HttpMethod = "post" }))
{
    <div class="jumbotron">
        @Html.TextArea("inputTextArea", "AJAX", new { @class = "form-control", rows = 10 })
    </div>
    <button type="submit" name="action" value="getValue">Execute</button>
}

<hr />

@*@using (Html.BeginForm("Index", "Report", FormMethod.Post))
    {
        <button type="submit" name="action" value="getValue">Execute</button>
    }*@

@Scripts.Render("~/scripts/jquery-1.10.2.min.js")
@Scripts.Render("~/scripts/jquery.unobtrusive-ajax.min.js")

